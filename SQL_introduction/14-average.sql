-- Script that computes the score average of all records in the table second_table
-- Calculates the mathematical average and names the result column as 'average'
SELECT AVG(score) AS average FROM second_table;
