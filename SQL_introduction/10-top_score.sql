-- Script that lists all records of the table second_table sorted by score
-- Displays score and name columns ordered from highest to lowest score
SELECT score, name FROM second_table ORDER BY score DESC;
